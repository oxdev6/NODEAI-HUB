terraform {
  required_version = ">= 1.4.0"
}

# Providers are intentionally minimal to keep the skeleton portable.
# Configure providers (kubernetes/helm/cloud) in your environment or module as needed.


